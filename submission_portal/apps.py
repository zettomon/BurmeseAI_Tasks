from django.apps import AppConfig


class SubmissionPortalConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'submission_portal'
    verbose_name = "Data Submission Portal"
